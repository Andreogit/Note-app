import 'package:flutter/material.dart';

class PopupMenuDivider extends StatelessWidget {
  const PopupMenuDivider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const PopupMenuDivider();
  }
}
